type Global @entity(immutable: false) {
  id: ID!
  totalFeesIn: BigInt!
  totalFeesOut: BigInt!
}

type Mech @entity(immutable: false) {
  id: ID! # agent address
  totalEarned: BigInt!
  totalWithdrawn: BigInt!
}
