{
  "name": "olas-agents",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --studio olas-agents",
    "create-local": "graph create --node http://localhost:8020/ olas-agents",
    "remove-local": "graph remove --node http://localhost:8020/ olas-agents",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 olas-agents",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.97.1",
    "@graphprotocol/graph-ts": "^0.38.1"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
