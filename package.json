{
  "name": "pearl-staking-rewards-history",
  "license": "UNLICENSED",
  "scripts": {
    "generate-manifests": "node scripts/generate-manifests.js",
    "codegen": "graph codegen subgraph.gnosis.yaml",
    "build": "graph build",
    "deploy-gnosis": "graph deploy --network gnosis --studio pearl-staking-rewards-history subgraph.gnosis.yaml",
    "deploy-base": "graph deploy --network base --studio olas-base-staking-rewards-history subgraph.base.yaml",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.82.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
